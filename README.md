# Organise-linkedIn-saved-posts
This project contains a Chrome extension content script that extracts data from LinkedIn saved posts. It automatically scrolls, clicks the “Show more results” button when required, and collects post details such as text content and post URLs from dynamically loaded DOM elements.
